{
  "name": "sol-meme-risk-score",
  "version": "1.0.0",
  "description": "Solana Meme Token Risk Score System based on Cloudflare Worker",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler publish",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "itty-router": "^3.0.12"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "wrangler": "^4.20.0"
  }
}
